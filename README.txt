We will be using python to connect to our sql data base. 

The first cell is used to import all the important module that are going to be used in the project.

The second cell is used to establish coonection to our server.

The third cell is used to create a database if not created earlier.

Note the above steps are required to run only first time and can be skipped later on.

The fourth cell is used to connect to our databse.

The fith cell is where query is excuted.

The sixth and seventh cell are sample queries.

The eighth cell is used to output the data in the form of list.

The nineth cell outputs the same data in the form of dictionary.